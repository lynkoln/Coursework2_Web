@page
@model ParkingSystem.Pages.Payments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Payment[0].TimeOfPayment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payment[0].Total)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Payment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TimeOfPayment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ReceiptNo">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ReceiptNo">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ReceiptNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
