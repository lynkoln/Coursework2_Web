@page
@model ParkingSystem.Pages.Pricings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.Pricing[0].Period)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pricing[0].Price)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pricing) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Period)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Period">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Period">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Period">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
